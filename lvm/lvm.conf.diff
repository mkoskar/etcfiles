--- lvm.conf.orig
+++ lvm.conf
@@ -263,7 +263,7 @@ Hunk #1, lvm.conf devices {
     # to 1, discards will only be issued if both the storage and kernel provide
     # support.
     # 1 enables; 0 disables.
-    issue_discards = 0
+    issue_discards = 1
 }
 
 # This section allows you to configure the way in which LVM selects
